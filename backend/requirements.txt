# Mockly FastAPI ML Backend — Production Requirements
# Pinned for stability on Python 3.11

# ── Web framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── ML / Speech ────────────────────────────────────────────
openai-whisper==20231117
torch==2.2.2             # CPU-only build is fine (no GPU needed)
torchaudio==2.2.2

# ── Audio feature extraction ───────────────────────────────
imageio-ffmpeg==0.5.1    # Ships its own ffmpeg binary, no system install needed
librosa==0.10.2          # Fallback for edge-case WAV decoding
numpy==1.26.4

# ── NLP / Embeddings ───────────────────────────────────────
sentence-transformers==3.0.1

# ── ML utilities ───────────────────────────────────────────
scikit-learn==1.5.1
joblib==1.4.2
pandas==2.2.2

# ── Misc ───────────────────────────────────────────────────
python-dotenv==1.0.1     # Loads .env in local dev (optional, safe to include)
